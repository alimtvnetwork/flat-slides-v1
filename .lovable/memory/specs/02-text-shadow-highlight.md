# Spec: Highlight Text Shadow (verbatim)

> for the yellow color, it looks like it has a, like, glowing effect where it should have been or should have a box shadow effect.
>
> specific CSS
> text-shadow: rgb(0 0 0) 1px 0.7px 0px;

Applied to `.hl` in `src/styles.css`. Must be very precise — not too much, not too less.
