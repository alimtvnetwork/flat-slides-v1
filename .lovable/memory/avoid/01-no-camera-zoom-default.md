# Avoid: Camera-Zoom as Default

User dislikes camera-zoom on every slide. Default transition is `fade`.
Lists / steps / timeline must never scale or zoom — only opacity + ≤16px
translate. Reserve `camera-zoom` for opt-in hero/title moments only.
