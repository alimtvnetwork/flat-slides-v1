# Avoid: Glow / Blur on `.hl`

Never add blur layers, drop-shadow filters, or multi-layer glow shadows
to the `.hl` highlight class. The spec is a single crisp text-shadow —
see `decisions/02-highlight-text-shadow.md`.
