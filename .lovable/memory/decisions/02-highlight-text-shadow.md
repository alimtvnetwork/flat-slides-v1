# Decision: `.hl` Text Shadow

`src/styles.css` `.hl` uses exactly:
```css
text-shadow: rgb(0 0 0) 1px 0.7px 0px;
```
Crisp ink-stamp. No blur, no glow, no multi-layer shadows. `.hl-pill` is
separate and untouched.
