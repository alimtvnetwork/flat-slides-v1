# Decision: Clickable Step Targets

`useStepJump` hook in `RenderSlide.tsx` makes:
- `StepsSlide` rows clickable
- `TimelineSlide` milestones clickable

Each click jumps to that step's URL and plays the `zoom` sound.
